{
  "name": "cleanair-sensor-flow",
  "description": "flow sensor utilities",
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "test": "deno test -A $(fd deno)"
  },
  "dependencies": {
    "cleanair-sensor-common": "workspace:^0",
    "date-fns": "2.28.0",
    "invariant": "^2.2.4"
  },
  "devDependencies": {
    "@types/invariant": "^2.2.35",
    "cleanair-tsconfig": "workspace:^0"
  }
}
