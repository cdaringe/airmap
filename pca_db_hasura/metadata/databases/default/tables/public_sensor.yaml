table:
  name: sensor
  schema: public
select_permissions:
  - role: readonly
    permission:
      columns:
        - id
        - name
        - sensor_type_id
        - latitude
        - longitude
        - description
      filter: {}
